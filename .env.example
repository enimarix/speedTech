# Copy to .env for local (non-Docker) runs. Compose sets these itself.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/speedtech
PORT=3000

# Optional — enables the AI layer (SPEC §8, milestone M6). Everything else works without it.
# ANTHROPIC_API_KEY=
